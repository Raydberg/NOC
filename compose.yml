services:
  mongo-db:
    image: mongo:8.0.19
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASS}
    volumes:
      - db_data:/data/db
    ports:
      - 27017:27017


volumes:
  db_data: